apiVersion: v1
kind: Namespace
metadata:
  name: souverix-autorite
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: souverix-autorite
  namespace: souverix-autorite
  labels:
    app: souverix-autorite
    component: autorite
    platform: souverix
spec:
  replicas: 2
  selector:
    matchLabels:
      app: souverix-autorite
  template:
    metadata:
      labels:
        app: souverix-autorite
        component: autorite
        platform: souverix
    spec:
      containers:
      - name: autorite
        image: ghcr.io/dasmlab/autorite:latest
        imagePullPolicy: Always
        ports:
        - name: 8081
          containerPort: 8081
          protocol: TCP
        - name: health
          containerPort: 8081
          protocol: TCP

        env:
        - name: LOG_LEVEL
          value: "info"
        - name: COMPONENT
          value: "autorite"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: souverix-autorite
  namespace: souverix-autorite
  labels:
    app: souverix-autorite
    component: autorite
    platform: souverix
spec:
  type: ClusterIP
  ports:
  - name: 8081
    port: 8081
    targetPort: 8081
    protocol: TCP
  - name: health
    port: 8081
    targetPort: 8081
    protocol: TCP

  selector:
    app: souverix-autorite
