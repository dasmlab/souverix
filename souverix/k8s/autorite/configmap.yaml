apiVersion: v1
kind: ConfigMap
metadata:
  name: souverix-autorite-config
  namespace: souverix-autorite
  labels:
    app: souverix-autorite
    component: autorite
    platform: souverix
data:
  LOG_LEVEL: "info"
  COMPONENT: "autorite"
  # Add component-specific configuration here
