apiVersion: v1
kind: ConfigMap
metadata:
  name: souverix-coeur-config
  namespace: souverix-coeur
  labels:
    app: souverix-coeur
    component: coeur
    platform: souverix
data:
  LOG_LEVEL: "info"
  COMPONENT: "coeur"
  # Add component-specific configuration here
